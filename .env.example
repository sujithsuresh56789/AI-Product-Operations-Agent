# Optional. If unset, the agent runs on a deterministic offline stub LLM
# (see app/agent/llm.py) so the whole project works out of the box with
# no API key. Set this to use real Claude responses.
ANTHROPIC_API_KEY=
